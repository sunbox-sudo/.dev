# This is my repo setup
My Dev Enviormnt
That i use for set up my computers


# Install 
start with install git
```sudo pacman -S git```

clone the git repo 
```git clone https://github.com/sunbox-sudo/.dev.git```

cd to the directory 
```cd .dev```

altneatives for step 4 
alt 1 run instal script 
```runs/install```

alt 2 inatal apps you need manuly (not recomended)
```runs/{Select self}```
